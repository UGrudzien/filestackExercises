import { Security, StoreOptions } from '../client';
/**
 *
 * @private
 * @param session
 * @param url
 * @param opts
 * @param token
 * @param security
 */
export declare const storeURL: (session: any, url?: string, opts?: StoreOptions, token?: any, security?: Security) => Promise<{}>;
